// ignore_for_file: constant_identifier_names

enum ProductFormMode { create, edit }

enum UserRole { Admin, SheepVendor, AccessoryVendor, Customer }

enum RequestStatus { initial, loading, success, failure }

enum HomeStatus {
  initial,
  loading,
  auctionsDone,
  productsDone,
  storesDone,
  completed,
}
